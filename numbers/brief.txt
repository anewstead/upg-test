2. A text file contains a list of numbers, one per line. Write ashell pipeline which will print out the five most commonlyoccurring numbers, one per line without surrounding whitespace, indescending order. Use of perl, awk, or any other programminglanguage should be avoided.3. Do the same in perl (or any programming language of your choice).Use of backticks, system() etc should be avoided.note. used javascript with node to load text file and execute script